import React from "react";

const MovieDetailsModal = ({
  id,
  title,
  overview,
  runtime,
  voteAverage,
  voteCount,
  poster
}) => {
  return (
    <React.Fragment>
      <ul>
        <li />
      </ul>
    </React.Fragment>
  );
};

export default MovieDetailsModal;
